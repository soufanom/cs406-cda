import matplotlib.pyplot as plt
import numpy as np

# Exercise 1: Generate random data for x and y coordinates
# Hint: Use numpy's np.random.rand() function to generate 50 data points for x and y.
# Replace the None with your code.
x = np.random.rand(50)
y = None # TODO: Generate y data points

# Exercise 2: Create a scatter plot
# Hint: Use plt.scatter() to create a scatter plot.
# Replace the None with your code to plot x and y with some basic aesthetics.
plt.scatter(None, None, c='blue', alpha=0.5, marker='o') # TODO: Replace None with x and y

# Exercise 3: Set the title of the plot
# Hint: Use plt.title() to add a title to your plot.
# Add a title to your plot.
plt.title(None) # TODO: Add a title inside the quotes

# Exercise 4: Label the x-axis and y-axis
# Hint: Use plt.xlabel() and plt.ylabel() to add labels to the x-axis and y-axis respectively.
plt.xlabel(None) # TODO: Label the x-axis
plt.ylabel(None) # TODO: Label the y-axis

# Exercise 5: Show the plot
# Hint: Use plt.show() to display the plot.
# Write the command to show the plot.
None # TODO: Use plt.show() to display the plot
